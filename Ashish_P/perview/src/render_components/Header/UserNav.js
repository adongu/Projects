import React from 'react';
import { withRouter } from 'react-router-dom';
import { } from 'react-bootstrap';

const UserNav = () => {
  const showDropDown = () => {
    // make class active and selected
  }

  return (
    <div>
      <div>
      </div>
    </div>
  )
}

export default withRouter(UserNav);
